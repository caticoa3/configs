[allowlist]
  description = "Allowlist for known false positives"
  paths = [
    # Conda environment files contain package version strings in the format
    # package=version=build_hash which gitleaks misidentifies as API keys
    '''.config/conda/environment_all\.yml''',
  ]
